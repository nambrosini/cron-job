[package]
name = "cron-job"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A cronjob implementation using the cron scheduler"
readme = "README.md"
repository = "https://github.com/nambrosini/cron-jobs"
keywords = ["cron", "cronjob", "cronjobs"]
categories = ["cronjob", "cronjobs"]

[dependencies]
chrono = "0.4.24"
cron = "0.12.0"
